name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.18.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Test
        run: pnpm test

      - name: Type check
        run: pnpm typecheck

      - name: Lint
        run: pnpm lint

      - name: Apply review test formatting
        run: |
          git fetch origin feature/22-quality-guard
          git checkout -B feature/22-quality-guard FETCH_HEAD
          pnpm prettier packages/quality/quality.spec.mjs --write
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add packages/quality/quality.spec.mjs
          git commit -m "style: review回帰testをPrettierで整形する"
          git push origin HEAD:feature/22-quality-guard
          exit 1

      - name: Format check
        run: pnpm format:check

      - name: Build Core
        run: pnpm build

      - name: Test GitHub package
        run: pnpm --dir packages/github test

      - name: Type check GitHub package
        run: pnpm --dir packages/github typecheck

      - name: Build GitHub package
        run: pnpm --dir packages/github build

      - name: Install Quality package dependencies
        run: pnpm --dir packages/quality install --no-frozen-lockfile

      - name: Test Quality package
        run: pnpm --dir packages/quality test

      - name: Type check Quality package
        run: pnpm --dir packages/quality typecheck

      - name: Build Quality package
        run: pnpm --dir packages/quality build
